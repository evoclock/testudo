# Test command for the pre-commit-verify gate (~/agent-infra/hooks/pre_commit_verify.py).
# testudo declares pytest in [project.optional-dependencies] dev, so `uv run pytest`
# alone cannot find it; the --extra is required. First non-comment line is the command.
uv run --extra dev pytest -q
